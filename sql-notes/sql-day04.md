# Day 4 — 환경 + Git + CLI + DB 통합 보강

## 1.환경점검
- Homebrew 패키지: OK / 누락 항목:
- VSCode 확장 설치: …

## 2.Git 설정
- user.name / email: …
- SSH key 연결 결과: …
- alias 목록: st, lg, br, co …

## 3. CLI 미션
- ls, cd, grep 등 결과 요약 (10개 모두 성공 / N 개 실패)

## 4. DB 테이블 생성
